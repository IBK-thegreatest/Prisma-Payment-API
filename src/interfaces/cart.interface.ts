export interface CartData {
    cartId: string
    userId: string
    productName: string
    quantity: number
}

export interface Cart {
    cartId: string
    userId: string
    productId: string
    quantity: number
}